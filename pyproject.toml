[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "advent2021"
version = "0.0.1"
dependencies = [
  "typer"
]

[project.scripts]
advent2021 = "main:main"
